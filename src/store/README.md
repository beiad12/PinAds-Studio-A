# store

Zustand stores (client/session state) + React Query hooks (async state backed by
`storage`). UI code talks to modules exclusively through this layer.
Implemented starting Phase 2/3 — see `docs/ARCHITECTURE.md`.
